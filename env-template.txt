# ==========================================
# FundIQ MVP - Environment Variables Template
# ==========================================
#
# INSTRUCTIONS:
# 1. Copy this file's content into a new file named `.env.local` in the FundIQ directory
# 2. Copy this file's content into a new file named `.env` in the backend directory
# 3. Replace all placeholder values with your actual Supabase credentials
# 4. NEVER commit these files to version control
#
# ==========================================

# ==========================================
# FRONTEND (.env.local)
# ==========================================
# Create a file named `.env.local` in the FundIQ root directory with:

NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-public-key-here
NEXT_PUBLIC_PARSER_API_URL=http://localhost:8000
NEXT_PUBLIC_API_URL=http://localhost:8000

# PAR-265: deployment environment indicator badge (EnvBadge).
# Set to the correct value in Vercel project settings per environment:
#   Production environment → prod
#   Preview environment    → preview
#   Staging branch deploy  → staging
# Local dev falls back to "preview" when unset.
NEXT_PUBLIC_APP_ENV=preview

# ==========================================
# BACKEND (backend/.env)
# ==========================================
# Create a file named `.env` in the backend directory with:

SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ==========================================
# MUSA INTEGRATION (backend/.env) — REQUIRED, PER ENVIRONMENT
# ==========================================
# PAR-59: same backend image is deployed to every environment (staging,
# prod, ...) unchanged — these two MUST be set to THIS environment's own
# URLs on each Cloud Run service. There is no correct shared default:
# if left unset, links sent to Musa (or the internal team-notify) silently
# point at the wrong environment instead of failing loudly.

# This environment's own backend URL (Cloud Run service URL). Used to
# build status_url/pdf_url in webhook payloads sent to Musa.
API_BASE_URL=https://your-backend-service-url

# This environment's own frontend URL. Used only for the internal
# unparseable-file team-notify POST to /api/request-parser.
PARITY_FRONTEND_URL=https://your-frontend-url

# Musa-issued webhook target + shared auth token (same value in every
# environment Musa hasn't changed it for; get from Musa directly).
MUSA_WEBHOOK_URL=https://musa-provided-webhook-url
MUSA_WEBHOOK_AUTH_TOKEN=musa-provided-bearer-token

# ==========================================
# WHERE TO FIND THESE VALUES
# ==========================================
#
# 1. Log into your Supabase dashboard: https://supabase.com/dashboard
# 2. Select your project
# 3. Go to Settings → API
# 4. Copy:
#    - Project URL → use for SUPABASE_URL and NEXT_PUBLIC_SUPABASE_URL
#    - anon public key → use for NEXT_PUBLIC_SUPABASE_ANON_KEY
#    - service_role key → use for SUPABASE_SERVICE_ROLE_KEY
#
# ⚠️  IMPORTANT:
# - Keep service_role key SECRET (only use in backend)
# - The anon key is safe to use in frontend
# - Never commit .env files to Git
#
# ==========================================


